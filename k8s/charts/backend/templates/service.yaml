apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurant-backend.fullname" . }}
  labels:
    {{- include "restaurant-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "restaurant-backend.selectorLabels" . | nindent 4 }}
